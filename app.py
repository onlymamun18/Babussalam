import streamlit as st
import pandas as pd

# অ্যাপের টাইটেল ও কনফিগারেশন
st.set_page_config(page_title="বাবুস সালাম মাদরাসা", page_icon="🕌", layout="wide")

# ডিজাইন সুন্দর করার জন্য কিছু CSS
st.markdown("""
    <style>
    .main { background-color: #f5f7f9; }
    .stButton>button { width: 100%; border-radius: 5px; height: 3em; background-color: #007bff; color: white; }
    </style>
    """, unsafe_allow_html=True)

st.title("🕌 বাবুস সালাম মাদরাসা ডিজিটাল ম্যানেজমেন্ট")

# সাইডবার মেনু
with st.sidebar:
    st.image("https://www.gstatic.com/images/branding/product/2x/forms_96dp.png", width=100)
    st.title("মেনু")
    choice = st.radio("পেজ সিলেক্ট করুন:", ["ড্যাশবোর্ড", "ছাত্র-শিক্ষক প্রোফাইল", "হাজিরা সিস্টেম", "রেজাল্ট পাবলিশ", "নোটিশ বোর্ড", "কিতাবখানা"])

# ১. ড্যাশবোর্ড
if choice == "ড্যাশবোর্ড":
    st.subheader("স্বাগতম! বাবুস সালাম মাদরাসার ডিজিটাল পোর্টালে।")
    col1, col2, col3 = st.columns(3)
    col1.metric("মোট ছাত্র", "১২০")
    col2.metric("মোট শিক্ষক", "১০")
    col3.metric("আজকের হাজিরা", "৯৫%")

# ২. প্রোফাইল (তথ্য আপডেট করার সুবিধা)
elif choice == "ছাত্র-শিক্ষক প্রোফাইল":
    st.header("👥 নতুন প্রোফাইল তৈরি করুন")
    with st.form("profile_form"):
        name = st.text_input("পুরো নাম")
        role = st.selectbox("পদবী", ["ছাত্র", "শিক্ষক", "স্টাফ"])
        mobile = st.text_input("মোবাইল নম্বর")
        blood_group = st.selectbox("রক্তের গ্রুপ", ["A+", "A-", "B+", "B-", "O+", "O-", "AB+", "AB-"])
        submitted = st.form_submit_button("তথ্য সেভ করুন")
        if submitted:
            st.success(f"সফলভাবে {name}-এর তথ্য ডাটাবেসে জমা হয়েছে।")

# ৩. হাজিরা সিস্টেম
elif choice == "হাজিরা সিস্টেম":
    st.header("📝 আজকের হাজিরা")
    date = st.date_input("তারিখ সিলেক্ট করুন")
    st.info("ছাত্রদের নামের তালিকা:")
    # নমুনা তালিকা
    students = ["আব্দুল্লাহ", "ওসমান", "ইব্রাহিম", "হাসান"]
    for s in students:
        st.checkbox(s)
    if st.button("হাজিরা নিশ্চিত করুন"):
        st.success("হাজিরা সফলভাবে নেওয়া হয়েছে।")

# ৪. রেজাল্ট পাবলিশ
elif choice == "রেজাল্ট পাবলিশ":
    st.header("📊 পরীক্ষার ফলাফল আপলোড")
    student_name = st.text_input("ছাত্রের নাম")
    subject = st.text_input("বিষয়")
    marks = st.number_input("প্রাপ্ত নম্বর", 0, 100)
    if st.button("ফলাফল সেভ করুন"):
        st.balloons()
        st.success(f"{student_name}-এর রেজাল্ট আপডেট হয়েছে।")

# ৫. নোটিশ বোর্ড
elif choice == "নোটিশ বোর্ড":
    st.header("📢 নতুন নোটিশ দিন")
    title = st.text_input("নোটিশের শিরোনাম")
    msg = st.text_area("বিস্তারিত লিখুন")
    if st.button("পাবলিশ করুন"):
        st.warning("নোটিশটি সবার অ্যাপে প্রদর্শিত হচ্ছে।")

# ৬. কিতাবখানা
elif choice == "কিতাবখানা":
    st.header("📚 কিতাব ও লাইব্রেরি")
    st.write("এখানে কিতাবের পিডিএফ লিঙ্ক বা ফাইল যোগ করা যাবে।")
    st.file_uploader("পিডিএফ ফাইল আপলোড করুন")
