import streamlit as st
import pandas as pd

# অ্যাপের প্রাথমিক কনফিগারেশন
st.set_page_config(page_title="বাবুস সালাম ইসলামি একাডেমি", page_icon="🕌", layout="wide")

# ডিজাইন উন্নত করার জন্য CSS
st.markdown("""
    <style>
    .main { background-color: #f0f2f6; }
    .stButton>button { width: 100%; border-radius: 8px; background-color: #008080; color: white; height: 3em; }
    h1 { color: #008080; text-align: center; font-family: 'SolaimanLipi', sans-serif; }
    </style>
    """, unsafe_allow_html=True)

# মাদরাসার নাম
st.title("🕌 বাবুস সালাম ইসলামি একাডেমি")
st.markdown("---")

# সাইডবার মেনু (বাংলায়)
with st.sidebar:
    st.header("মেনু অপশন")
    choice = st.radio("যেখানে যেতে চান:", [
        "🏠 ড্যাশবোর্ড", 
        "👤 প্রোফাইল তৈরি (ছাত্র/শিক্ষক)", 
        "📝 ডিজিটাল হাজিরা", 
        "📊 রেজাল্ট শিট", 
        "📢 নোটিশ বোর্ড", 
        "💰 ফি কালেকশন", 
        "📚 কুরআন ও কিতাবখানা"
    ])

# ১. ড্যাশবোর্ড
if choice == "🏠 ড্যাশবোর্ড":
    st.subheader("স্বাগতম! বাবুস সালাম ইসলামি একাডেমি ডিজিটাল পোর্টালে।")
    col1, col2, col3 = st.columns(3)
    col1.metric("মোট ছাত্র", "১২০ জন")
    col2.metric("মোট শিক্ষক", "১০ জন")
    col3.metric("আজকের উপস্থিতি", "৯৫%")

# ২. প্রোফাইল তৈরি (বিস্তারিত তথ্যসহ)
elif choice == "👤 প্রোফাইল তৈরি (ছাত্র/শিক্ষক)":
    st.header("👤 নতুন প্রোফাইল নিবন্ধন")
    with st.form("registration_form"):
        col1, col2 = st.columns(2)
        
        with col1:
            role = st.selectbox("ধরন", ["ছাত্র", "শিক্ষক", "স্টাফ"])
            id_no = st.text_input("আইডি (ID) নম্বর")
            full_name = st.text_input("পুরো নাম")
            father_name = st.text_input("পিতার নাম")
            mother_name = st.text_input("মাতার নাম")
            
        with col2:
            mobile = st.text_input("মোবাইল নম্বর")
            address = st.text_area("বর্তমান ঠিকানা")
            blood_group = st.selectbox("রক্তের গ্রুপ", ["জানা নেই", "A+", "A-", "B+", "B-", "O+", "O-", "AB+", "AB-"])
            photo = st.file_uploader("ছবি আপলোড করুন", type=["jpg", "jpeg", "png"])
        
        submit_btn = st.form_submit_button("তথ্য সেভ করুন")
        
        if submit_btn:
            if full_name and id_no:
                st.success(f"আলহামদুলিল্লাহ! {full_name}-এর প্রোফাইল তৈরি হয়েছে।")
                if photo:
                    st.image(photo, width=150, caption="আপলোড করা ছবি")
                st.write(f"**পিতা:** {father_name} | **মাতা:** {mother_name}")
                st.write(f"**ঠিকানা:** {address}")
            else:
                st.error("দয়া করে নাম এবং আইডি নম্বরটি লিখুন।")

# ৩. ডিজিটাল হাজিরা
elif choice == "📝 ডিজিটাল হাজিরা":
    st.header("📝 প্রতিদিনের হাজিরা")
    date = st.date_input("তারিখ")
    student_id = st.text_input("ছাত্রের আইডি লিখুন")
    status = st.radio("উপস্থিতি", ["উপস্থিত", "অনুপস্থিত", "ছুটিতে"])
    if st.button("হাজিরা জমা দিন"):
        st.success(f"আইডি {student_id}-এর হাজিরা সফলভাবে নেওয়া হয়েছে।")

# ৪. রেজাল্ট শিট
elif choice == "📊 রেজাল্ট শিট":
    st.header("📊 পরীক্ষার ফলাফল")
    s_id = st.text_input("ছাত্রের আইডি")
    exam = st.selectbox("পরীক্ষার নাম", ["মাসিক পরীক্ষা", "সাময়িক পরীক্ষা", "বার্ষিক পরীক্ষা"])
    marks = st.number_input("প্রাপ্ত নম্বর", 0, 100)
    if st.button("ফলাফল আপডেট করুন"):
        st.balloons()
        st.success("ফলাফল ডাটাবেসে সেভ হয়েছে।")

# ৫. নোটিশ বোর্ড
elif choice == "📢 নোটিশ বোর্ড":
    st.header("📢 নতুন নোটিশ পোস্ট করুন")
    n_title = st.text_input("নোটিশের শিরোনাম")
    n_body = st.text_area("বিস্তারিত লিখুন")
    if st.button("নোটিশ জারি করুন"):
        st.warning(f"নোটিশ: {n_title} পাবলিশ করা হয়েছে।")

# ৬. ফি কালেকশন
elif choice == "💰 ফি কালেকশন":
    st.header("💰 বেতন ও অনুদান সংগ্রহ")
    f_id = st.text_input("ছাত্রের আইডি")
    amount = st.number_input("টাকার পরিমাণ (টাকা)", min_value=0)
    month = st.selectbox("মাসের নাম", ["জানুয়ারি", "ফেব্রুয়ারি", "মার্চ", "এপ্রিল", "মে", "জুন", "জুলাই", "আগস্ট", "সেপ্টেম্বর", "অক্টোবর", "নভেম্বর", "ডিসেম্বর"])
    if st.button("রসিদ তৈরি করুন"):
        st.success(f"সফলভাবে {amount} টাকা গ্রহণ করা হয়েছে।")

# ৭. কুরআন ও কিতাবখানা
elif choice == "📚 কুরআন ও কিতাবখানা":
    st.header("📚 ডিজিটাল কিতাবখানা")
    st.write("আপনার প্রয়োজনীয় কিতাব আপলোড করুন বা এখান থেকে পড়ুন।")
    st.file_uploader("পিডিএফ (PDF) ফাইল আপলোড")
    st.markdown("---")
    st.write("📖 **সংরক্ষিত কিতাবসমূহ:**")
    st.info("এখনো কোনো কিতাব আপলোড করা হয়নি।")
