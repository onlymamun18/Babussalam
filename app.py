import streamlit as st

st.set_page_config(page_title="মাদরাসা অ্যাপ", layout="wide")
st.title("🕌 মাদরাসা ম্যানেজমেন্ট সিস্টেম")

menu = ["প্রোফাইল", "হাজিরা", "রেজাল্ট", "নোটিশ", "ফি", "কিতাব"]
choice = st.sidebar.selectbox("মেনু", menu)

if choice == "প্রোফাইল":
    st.header("👥 প্রোফাইল আপডেট")
    st.text_input("নাম")
    st.selectbox("পদবী", ["ছাত্র", "শিক্ষক"])
    if st.button("সেভ"):
        st.success("তথ্য সেভ হয়েছে!")
else:
    st.info(f"আপনি এখন {choice} সেকশনে আছেন।")
